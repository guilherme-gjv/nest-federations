type User {
  id: ID!
  name: String!
  email: String!
  createdAt: String!
  updatedAt: String
}

input CreateUserInput {
  name: String!
  email: String!
  password: String!
}

type Query {
  users: [User!]!
  user(id: ID!): User
}

type Mutation {
  createUser(data: CreateUserInput!): User!
}
